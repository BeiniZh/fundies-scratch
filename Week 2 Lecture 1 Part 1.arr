use context starter2024
# we called those "names", for example the raidus below is a 'name'. We can read the following line as'raidus is defined to be 9'

radius = 9 #this complete statement is a definition

books-quantity=5

weight = 70

height = 180

# expression, code that computes something
bmi = weight * height


#|这依旧没有结果，除非我们run一个bmi才会有结果，如下
   weight and height should be above the equation of bmi = weight + height
   code follows the order from top to bottom, the sequences matter.
|#

weight * height # you will have some anser with this code

height-b=170
height-c=180


height-list = [160, 170, 180, 190, 155]
# we use list when we have too many people to store the information. in python we called it a value.

#statement--> code that instructs rather than computes.
# definition is a statement

